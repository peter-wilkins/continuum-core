# Explicit Inputs over Optional Defaults

Continuum Core avoids optional public input fields and implicit default values except where omission is genuinely harmless and unsurprising. Capture depends on context, so missing facts should be represented explicitly instead of silently defaulted by the library.
